apiVersion: aws.managed.openshift.io/v1alpha1
kind: AccountClaim
metadata:
  name: example-accountclaim
spec:
  legalEntity:
    name:
    id:
  awsCredentialSecret:
    name:
    namespace:
  aws:
    regions:
    - name:
  # Blank by default, the claim controller will populate this
  accountLink: ""
